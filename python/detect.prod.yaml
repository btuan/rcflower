# Production config for the Pi's rcflower-detect systemd service (see
# deploy/systemd/rcflower-detect.service). Only lists what differs from
# detect.yaml's defaults; every other key still falls back to detect.py's
# built-in DEFAULTS.

headless: true
camera: 0
classes: person
model: yolov8n_ncnn_model_224/model.ncnn.param
labels: yolov8n_ncnn_model_224/metadata.yaml
