# Dev server config. Override machine-specific values in .env.local (gitignored).

# Comma-separated hostnames Vite's dev server will accept (in addition to
# localhost). Set this on machines served behind a proxy / tunnel, e.g.
# ALLOWED_HOSTS=kirwinpi.tail100881.ts.net
ALLOWED_HOSTS=
